# Base URL of the NestJS backend (contact form, and once you go dynamic, the
# rest of the data endpoints too). Leave empty to run the frontend fully
# static against the JSON files in /data.
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
